# ToolSuite SEO and Accessibility QA — 2026-08-18

## Routes checked

The homepage and `/calculator/age` were opened directly in the active preview. Both routes rendered successfully with the dark-first theme.

## Homepage findings

The document title is `ToolSuite — Free Online Productivity Tools`. The page exposes one dominant H1, visible category sections, a search input with an accessible search hint, a visible exact `30` tool count, category navigation, practical guides, and all 30 tool names in crawlable page text. Homepage navigation and tool cards remain visible and usable at the desktop viewport.

## Age Calculator findings

The direct route title is `Age Calculator — Exact Age in Years, Months, Days & Hours | ToolSuite`. The route renders a dominant `Age Calculator` H1, breadcrumb controls, labelled date inputs, a visible methodology section titled `How Age is Calculated`, comparison tables with headers, extended calculations, and related calculator links. The page rendered without a route error.

## Accessibility and CWV-sensitive observations

The viewport metadata was corrected to remove `maximum-scale=1`, preserving user zoom. The inspected routes expose labelled inputs, visible focus-capable controls, semantic headings, table headers, and readable contrast in dark mode. No oversized hero media or layout-shifting image was observed in the inspected first viewport. A full production Lighthouse/PageSpeed measurement remains a post-deployment measurement rather than a local source assertion.

## SEO metadata changes validated in source and tests

All 30 tools generate unique titles. Custom and generated descriptions are constrained to a concise 160-character maximum. Canonical URLs use `https://toolsuite-bice.vercel.app`. Robots and sitemap contain the same production origin, and the sitemap contains 31 URLs: the homepage plus exactly 30 tool routes. Twitter metadata uses `name` attributes, while Open Graph metadata uses `property` attributes.

## Validation results

The expanded Vitest suite passed with 21 tests. Production build passed. Lint passed. Static robots, sitemap, canonical, and social metadata assertions passed. Representative desktop screenshots passed for the homepage and Age Calculator route.

## Known non-SEO warning

The managed project status still reports pre-existing TypeScript errors under the archived `server/original-backend` surface, including missing `express-rate-limit` and `multer` types. These warnings are outside the active SEO/frontend build path and were not introduced by this SEO pass.
